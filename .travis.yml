language: node_js
node_js:
  - '8.12'
env:
  - NODE_ENV=travis
services:
  - postgresql
addons:
  postgresql: "10"
  apt:
    packages:
    - postgresql-10
    - postgresql-client-10
env:
  global:
  - PGPORT=5433
before_script:
  - psql -p 5433 -c 'create database jumbosmash_test;' -U postgres
  - psql -p 5433 -c 'create extension if not exists citext;' -U postgres
script:
  - cd ./src/server
