version: '3'
services:
  nginx:
    build: ./nginx
    links:
      - test-1
      - test-2
      - test-3
      - test-4
    ports:
    - "3003:3003"
  test-1:
    image: maxgreenwald/projectgem
    expose:
      - "3003"
    environment:
      NODE_ENV: production
  test-2:
    image: maxgreenwald/projectgem
    expose:
      - "3003"
    environment:
      NODE_ENV: production
  test-3:
    image: maxgreenwald/projectgem
    expose:
      - "3003"
    environment:
      NODE_ENV: production
  test-4:
    image: maxgreenwald/projectgem
    expose:
      - "3003"
    environment:
      NODE_ENV: production
