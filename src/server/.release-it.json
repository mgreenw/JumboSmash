{
  "git": {
    "tagName": "server-${version}",
    "tagAnnotation": "Release server ${version}",
    "commitMessage": "Release serer ${version}"
  },
  "github": {
    "release": true,
    "releaseName": "Release server ${version}"
  },
  "npm": {
    "publish": false
  }
}
